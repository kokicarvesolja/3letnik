\begin{tikzpicture}
  \begin{axis}[
    title={Energija mehurčka v odvisnosti od polmera in tlaka $[\mathrm{bar}]$},
    axis lines = left,
    xlabel = {\(\text{Polmer} \ [\si{\angstrom}]\)},
    ylabel = {\( \text{Energija} \ [\mathrm{eV}]\)},
    ymax=2,
    ymin=-1,
    legend pos = north west,
]

\addplot [
    domain=6:100, 
    samples=100, 
    color=blue,
    dotted,
    thick,
    mark=none,
]
{37.612 / (x^2) + 4 * pi * 2.212 * x^2 / (10^5)};
\addlegendentry{P = 0}

\addplot [
    domain=6:100, 
    samples=100, 
    color=black,
    ]
    {37.612 / (x^2) + 4 * pi * 2.212 * x^2 / (10^5) - 4 * pi * 6.241 * x^3 / (3 * 10^7)};
\addlegendentry{P = -1}

\addplot [
    domain=6:100, 
    samples=100, 
    color=red,
    dashed,
    thick,
    mark=none,
    ]
    {37.612 / (x^2) + 4 * pi * 2.212 * x^2 / (10^5) - 8 * pi * 6.241 * x^3 / (3 * 10^7)};
\addlegendentry{P = -2}

\addplot [
    domain=6:100, 
    samples=100, 
    color=green,
    dashdotted,
    thick,
    mark=none,
    ]
    {37.612 / (x^2) + 4 * pi * 2.212 * x^2 / (10^5) - 4 * pi * 6.241 * x^3 / (10^7)};
\addlegendentry{P = -3}

\addplot[
domain=0:6,
samples=2,
color=black,
] {1};

\draw [-{Stealth}] (axis cs:23,-0.25) node[right] {A} -- (axis cs:19,0.10);
\draw [-{Stealth}] (axis cs:85, 0.85) node[right] {B} -- (axis cs:74,0.5);
\end{axis}
\end{tikzpicture}
